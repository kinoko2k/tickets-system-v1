version: '3.8'

services:
  app:
    build: .
    container_name: tickets-system
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
      - ./config.json:/app/config.json
    restart: always
